RESUMEN SPRINT 4 — Cookr TFG
Generado: 2026-05-12 · Última actualización: 2026-05-15 (PERF Sprint 5 completado)
============================================================

BACKEND — NUEVOS ENDPOINTS
- COL-001   BE GET /api/recetas/guardadas — devuelve recetas guardadas del usuario autenticado
- COL-002   BE GET /api/recetas/mis-recetas — devuelve recetas creadas por el usuario autenticado
- COL-007   BE DELETE /api/recetas/:id — elimina receta propia verificando que el autor coincide (403 si no)
- API-013   BE POST /api/recetas — crea receta completa en MongoDB con foto base64, alérgenos y mapeo de dificultad
- SOC-001   BE POST /api/usuarios/:id/seguir — toggle seguir/dejar de seguir con arrays seguidos/seguidores en Usuario
- FOTO-001  BE imagenService.ts — si el usuario no sube foto, busca imagen automática en Pexels API y guarda URL + créditos
- FIX-001   BE express.json limit subido a 10 mb para aceptar fotos en base64 sin error 413

BACKEND — MEJORAS
- FEED-001  BE filtro excluirPropio en GET /api/recetas — no muestra recetas propias en el feed de home
- LIKE-001  BE toggle like único por usuario (array likes en Receta, un like por userId)
- COM-001   BE POST /api/recetas/:id/comentarios — guarda comentario en listaComentarios del documento Receta
- FIX-003   BE GET /recetas/foto-preview?query=xxx — endpoint ligero sin auth para preview de foto Pexels antes de publicar

FRONTEND — NUEVOS HOOKS Y SERVICIOS
- DEBT-009  FE useCrearReceta.ts — hook que orquesta detección de alérgenos, llamada al BE, limpieza del store y redirección
- COL-004   FE useRecetasGuardadas.ts + useMisRecetas.ts — TanStack Query conectados a los endpoints de colección
- COL-006   FE useEliminarReceta.ts — mutation TanStack Query que invalida caché tras eliminar
- FOTO-002  FE useFotoPexelsPreview.ts — hook que llama a /recetas/foto-preview para mostrar foto en la preview antes de publicar
-           FE recetasService.obtenerFotoPreview() — método HTTP para el endpoint de preview de Pexels

FRONTEND — VISTAS COMPLETAS (mock, integración BE en Fase 5)
- DESP-001  Vista /despensa — header editorial, lista de ingredientes con emojis, sheet añadir, dialog editar, autocompletado, detección de duplicados, Zustand store
- PERF-001  Vista /perfil — avatar, grupos de ajustes, dialog cambiar contraseña, dialog preferencias dietas/alérgenos, toggles de permisos, Zustand store
- CHAT-001  Vista /chat — burbujas usuario/IA, indicador pensando animado, chips sugerencias, barra input sticky, chatStore Zustand con mock 1.4s
- DISC-001  Vista /discover — header editorial, tarjeta destacada evento semanal, grid 2 columnas, chips categoría, búsqueda con debounce, estado vacío

FRONTEND — COLECCIÓN CONECTADA AL BACKEND
- COL-004   contenidoColeccion.tsx reemplaza mocks por hooks reales con skeleton animate-pulse durante carga
- COL-004   Tab inicial leído de ?tab=mis-recetas en la URL con useSearchParams (Suspense boundary añadido)
- COL-005   Bookmark funcional en pestaña Guardadas con actualización optimista y rollback en error
- COL-006   Botón ··· en pestaña Mis Recetas abre Sheet → Dialog confirmación → elimina receta real

FRONTEND — CREAR RECETA CONECTADO AL BACKEND
- CREAR-002 previsualizacionReceta.tsx usa useCrearReceta.publicar() en lugar de setTimeout mock
- CREAR-001 Tutorial activado por datos reales (useMisRecetas) en lugar de constante MOCK_ES_PRIMER_USUARIO
- FOTO-001  Si no se sube foto, la preview muestra la foto de Pexels con atribución superpuesta ("Buscando foto…" mientras carga)

FRONTEND — INGREDIENTES
- INGR-001  Autocompletado reemplaza 30 ingredientes mock por src/config/ingredientes.ts con 200+ ingredientes, aliases y alérgenos EU
- INGR-002  ingredientesService.ts (stub Edamam) + ingredientesCustomStore.ts (Zustand persist para ingredientes nuevos)
- INGR-003  useAutocompletadoIngredientes.ts — prioriza local (239 ingredientes) > custom store > fallback Edamam con debounce 300ms
- ALERG-002 detectarAlergenos.ts reescrito usando la misma fuente que el autocompletado (matching exacto por nombre y aliases)

FRONTEND — FUNCIONALIDAD SOCIAL
- DET-003   Botón Compartir funcional — Web Share API en móvil, fallback clipboard en escritorio, feedback visual 2s
- COM-001   Añadir comentario con actualización optimista local + router.refresh() en onSuccess
- DET-005   Estado vacío "Sé el primero en comentar" cuando no hay comentarios
- DET-006   Botón "Añadir a mi despensa" en tabsReceta filtra duplicados y añade al despensaStore con feedback 3s
- SOC-001   Botón Seguir/Siguiendo en detalle receta, oculto en recetas propias, estado inicial desde SSR
- UI-AF-002 Botón Eliminar en detalle receta visible solo para el autor de la receta

FRONTEND — MEJORAS UI
- UI-029    Tarjeta fantasma "+ Nueva receta" en grid de Mis Recetas con borde punteado brand → /crear-receta
- UI-AF-001 Botón ··· en tarjetaColeccion con círculo bg-white/20 backdrop-blur para mayor affordance
- UI-AF-003 seccionIngredientes rediseñada: card por ingrediente con dos filas (nombre / cantidad + unidad), 20 unidades disponibles
- UI-AF-004 seccionPasos con textarea auto-resize (crece con el contenido, sin scroll interno)

BACKEND — DESPENSA (Sprint 5)
- DESP-BE-001  BE Schema despensa — backend/src/types/despensa.ts (IItemDespensa), IUsuario extendido con despensa?: IItemDespensa[], itemDespensaSchema embebido en usuarioMongo.ts con campo despensa default []
- DESP-BE-002  BE despensaRepository.ts — obtener ($select), añadir ($push), editar ($set posicional despensa.$.campo), eliminar ($pull por _id). Devuelve array completo en cada operación
- DESP-BE-003  BE despensaService.ts + despensaController.ts — capa de negocio + validación de campos obligatorios + manejarError centralizado
- DESP-BE-004  BE despensa.routes.ts (GET / · POST / · PUT /:id · DELETE /:id, todas requerirAuth) registrado en app.ts como /api/despensa

FRONTEND — DESPENSA (Sprint 5)
- DESP-FE-001  FE despensaService.ts — 4 métodos HTTP con Authorization: Bearer, normalizar() convierte _id (MongoDB) a id (FE)
- DESP-FE-002  FE useMiDespensa.ts — useMiDespensa (useQuery, staleTime:0) + useAñadirItem + useEditarItem + useEliminarItem (useMutation con optimistic update + rollback completo)
- DESP-FE-003  FE contenidoDespensa.tsx usa hooks reales (isLoading spinner), sheetAnadirIngrediente.tsx recibe ingredientesExistentes como prop, despensaStore.ts vaciado, tabsReceta.tsx migrado a useMiDespensa + useAñadirItem

BACKEND — PERFIL (Sprint 5)
- PERF-BE-001  BE GET /api/usuarios/me — usuarioRepository.buscarPerfilPorId() con .select('nombre correo foto alergias preferencias proveedor'). usuariosService.obtenerPerfil() + controller + route (requerirAuth). Declarado antes de /:id para evitar que Express interprete "me" como id MongoDB
- PERF-BE-002  BE PUT /api/usuarios/me/contrasena — bcrypt.compare(actual) + bcrypt.hash(nueva, 10) + actualizarContrasena(). 400 si proveedor!=='local' o contraseña incorrecta. 404 si usuario no encontrado
- PERF-BE-003  BE PUT /api/usuarios/me/preferencias — body { dietas: string[], alergenos: string[] }, mapea dietas→preferencias y alergenos→alergias en MongoDB. Devuelve perfil actualizado (mismo shape que GET)

FRONTEND — PERFIL (Sprint 5)
- PERF-FE-001  FE perfilService.ts — 3 métodos HTTP: getMiPerfil, cambiarContrasena, actualizarPreferencias con Authorization: Bearer. PerfilRespuesta interface espejo del BE. Fix: import { apiClient } corregido (era default import, error TS)
- PERF-FE-002  FE usePerfil.ts — useMiPerfil (useQuery, staleTime 5min), useCambiarContrasena (useMutation), useActualizarPreferencias (useMutation con optimistic update completo: cancelQueries + setQueryData + rollback en onError + sync en onSuccess). extraerMensajeError() helper Axios
- PERF-FE-003  FE Connect — contenidoPerfil.tsx: nombre/email/foto de useSession(), dietas/alergenos/proveedor de useMiPerfil(), fila "Cambiar contraseña" oculta si proveedor==='google'. dialogCambiarContrasena.tsx: mutación real con errorServidor visible. dialogPreferenciasAlergenos: llama useMiPerfil() directamente (cache hit), draft sincronizado con [abierto, perfil]. perfilStore simplificado a solo permisos locales (ubicacion/camara/notificaciones)
- PERF-FIX-001 FE Fix dialog preferencias — chips activos no se mostraban al abrir el dialog. Causa: refetchOnMount:always + invalidateQueries en mount → perfil=undefined durante refetch → props vacías. Fix: staleTime 5min, eliminado useEffect invalidateQueries, dialog usa useMiPerfil() directo. tsc --noEmit: 0 errores

FIXES
- FIX-002   Suspense boundary en coleccion/page.tsx para que useSearchParams funcione correctamente en Next.js 14
- FIX-004   Dropdown de ingredientes movido fuera del div overflow-hidden para que no se corte visualmente
- FIX-005   next.config.mjs: images.pexels.com añadido a remotePatterns para permitir imágenes de Pexels
- FIX-006   Race condition en redirect tras publicar: router.push antes de limpiar() + useRef para guardia de montaje en preview

INFRAESTRUCTURA / CONFIG
- FOTO-002  heroReceta.tsx muestra atribución Pexels (fotógrafo + enlace) cuando fotoFuente === 'pexels' (obligatorio por licencia)
